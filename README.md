# spandec

> [!WARNING]
> **Deprecated.** spandec was merged into
> [spancodec](https://github.com/apstndb/spancodec) before its first
> release. Use spancodec; the `Decode` / `ToStruct` API is unchanged there.
